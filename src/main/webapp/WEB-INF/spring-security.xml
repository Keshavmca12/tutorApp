<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-4.0.xsd">

    <!-- enable use-expressions -->
    <http pattern="/resources/**" security="none" />
	<http auto-config="true" use-expressions="true">
		<!-- its convention to add ROLE_* to any role for using spring security -->
		 <intercept-url pattern="/login" access="permitAll"></intercept-url>
		  <intercept-url pattern="/submitUserDetails" access="permitAll"></intercept-url>
        <intercept-url pattern="/accessdenied" access="permitAll"></intercept-url>
        <!-- <intercept-url pattern="/**" access="hasAnyRole('student','teacher')"></intercept-url>  -->  <!--   hasRole('ROLE_STUDENT') -->
		 <intercept-url pattern="/student/**" access="hasRole('student')" />
        <intercept-url pattern="/teacher/**" access="hasRole('teacher')" />
        <intercept-url pattern="/admin/**" access="hasRole('admin')" />
        <intercept-url pattern="/**" access="hasAnyRole('student','teacher','admin')"></intercept-url> 
        
         <form-login  login-page="/login"
                     username-parameter="userName"
                     password-parameter="password"
                     login-processing-url="/j_spring_security_check"
                     authentication-success-handler-ref="customSuccessHandler"
                     authentication-failure-url="/login?error" /> 
	<!-- 	<form-login 
		    login-page="/login" 
		    default-target-url="/home" 
		    always-use-default-target="true" 
			login-processing-url="/j_spring_security_check"
			authentication-failure-url="/403" 
			username-parameter="userName"
			password-parameter="password" /> -->
		<!-- enable csrf protection -->
		  <csrf/>
	</http>
	
	<!-- Select users and user_roles from database -->
	<authentication-manager>
	 <!-- <authentication-provider>
            <user-service>
                <user name="keshav"  password="keshav"  authorities="ROLE_STUDENT" />
            </user-service>
        </authentication-provider> -->
        <authentication-provider user-service-ref="springUserDetailService"/>
      
	</authentication-manager>
	 <beans:bean id="customSuccessHandler" class="com.security.CustomSuccessHandler" />
	 <beans:bean id="springUserDetailService" class="com.service.SpringUserDetailService" />

</beans:beans>